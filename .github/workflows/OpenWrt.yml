name: fanchmwrt+kenzo

on:
  workflow_dispatch:

env:
  TZ: Asia/Shanghai

jobs:
  build:
    name: Build fanchmwrt Firmware (L2TP/IPsec PSK)
    runs-on: ubuntu-22.04

    steps:
      - name: Check Server Performance
        run: |
          echo "CPU物理数量: $(cat /proc/cpuinfo | grep "physical id" | sort | uniq | wc -l)"
          echo "CPU核心数量: $(nproc)"
          echo "硬盘数量: $(ls /dev/sd* | grep -v [1-9] | wc -l)" && df -hT

      - name: Initialization Environment
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo rm -rf /usr/share/dotnet /etc/apt/sources.list.d /usr/local/lib/android $AGENT_TOOLSDIRECTORY
          sudo -E apt-get -y purge azure-cli ghc* zulu* llvm* firefox google* dotnet* powershell openjdk* mongodb* moby* || true
          sudo -E apt-get -y update
          # L2TP/IPsec PSK 编译依赖（移除softethervpn依赖，新增IPsec依赖）
          sudo -E apt-get -y install build-essential clang flex bison g++ gawk gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev python3-distutils rsync unzip zlib1g-dev file wget qemu-utils curl git subversion libelf-dev autoconf automake libtool autopoint device-tree-compiler swig pkg-config libmnl-dev libidn2-dev libcap-dev libseccomp-dev libnftnl-dev ccache libpam0g-dev liblzma-dev libev-dev libreadline-dev libncurses-dev libssl-dev zlib1g-dev libgmp-dev libgcrypt-dev
          sudo systemctl daemon-reload
          sudo apt-get -y autoremove --purge
          sudo apt-get -y clean
          sudo timedatectl set-timezone "$TZ"
          echo "export TERM=linux" >> ~/.bashrc

      - name: Maximize build space
        uses: easimon/maximize-build-space@master
        with:
          swap-size-mb: 2048  # 增大swap，适配IPsec编译
          temp-reserve-mb: 100
          root-reserve-mb: 1024
          remove-dotnet: true
          remove-android: true
          remove-haskell: true
          remove-codeql: true
          remove-docker-images: true

      - name: Checkout
        uses: actions/checkout@main

      - name: Clone source & setup feeds (添加jell仓库)
        run: |
          git clone https://github.com/fanchmwrt/fanchmwrt openwrt
          cd openwrt
          # 清空原有feeds，重新配置（优先添加jell仓库）
          > feeds.conf.default
          echo "src-git jell https://github.com/kenzok8/jell" >> feeds.conf.default       # 核心：luci-app-ipsec-vpnd
          echo "src-git small https://github.com/kenzok8/small" >> feeds.conf.default     # 依赖补充
          echo "src-git nikki https://github.com/nikkinikki-org/OpenWrt-nikki.git" >> feeds.conf.default
          echo "src-git packages_official https://github.com/openwrt/packages.git;openwrt-24.10" >> feeds.conf.default
          echo "src-git luci_official https://github.com/openwrt/luci.git;openwrt-24.10" >> feeds.conf.default

      - name: Update & clean feeds
        run: |
          cd openwrt
          ./scripts/feeds update -a
          # 清理无关包，避免冲突
          rm -rf feeds/luci/applications/luci-app-mosdns
          rm -rf feeds/packages/net/{alist,adguardhome,mosdns,xray*,v2ray*,sing*,smartdns}
          rm -rf feeds/packages/utils/v2dat
          rm -rf feeds/packages/lang/golang
          git clone https://github.com/kenzok8/golang -b 1.26 feeds/packages/lang/golang
          # 显式安装jell仓库的ipsec-vpnd插件
          ./scripts/feeds install -p jell luci-app-ipsec-vpnd
          ./scripts/feeds install -p jell luci-i18n-ipsec-vpnd-zh-cn

      - name: Install feeds (完整安装)
        run: |
          cd openwrt
          ./scripts/feeds install -a

      - name: Generate correct config (L2TP/IPsec PSK)
        run: |
          cd openwrt
          export TERM=linux
          make defconfig
          cat > .config << 'EOF'
          CONFIG_TARGET_x86=y
          CONFIG_TARGET_x86_64=y
          CONFIG_TARGET_KERNEL_PARTSIZE=100
          CONFIG_TARGET_ROOTFS_PARTSIZE=700
          CONFIG_TARGET_ROOTFS_EXT4FS=n
          CONFIG_TARGET_ROOTFS_PERSIST_VAR=y
          CONFIG_TARGET_ROOTFS_TARGZ=y
          CONFIG_TARGET_IMAGES_GZIP=y
          # 基础工具
          CONFIG_PACKAGE_ddns-scripts=y
          CONFIG_PACKAGE_ddns-scripts-cloudflare=y
          CONFIG_PACKAGE_ddns-scripts_cloudflare.com-v4=y
          CONFIG_PACKAGE_luci-app-ddns=y
          CONFIG_PACKAGE_luci-app-udpxy=y
          CONFIG_PACKAGE_libopenssl=y
          CONFIG_PACKAGE_libreadline=y
          CONFIG_PACKAGE_libncurses=y
          CONFIG_PACKAGE_zlib=y
          # L2TP/IPsec PSK 核心配置（jell仓库）
          CONFIG_PACKAGE_luci-app-ipsec-vpnd=y
          CONFIG_PACKAGE_luci-i18n-ipsec-vpnd-zh-cn=y
          # IPsec/L2TP 依赖包（必需）
          CONFIG_PACKAGE_strongswan=y
          CONFIG_PACKAGE_strongswan-charon=y
          CONFIG_PACKAGE_strongswan-mod-aes=y
          CONFIG_PACKAGE_strongswan-mod-gcm=y
          CONFIG_PACKAGE_strongswan-mod-hmac=y
          CONFIG_PACKAGE_strongswan-mod-kernel-netlink=y
          CONFIG_PACKAGE_strongswan-mod-md5=y
          CONFIG_PACKAGE_strongswan-mod-sha2=y
          CONFIG_PACKAGE_strongswan-mod-socket-default=y
          CONFIG_PACKAGE_strongswan-mod-stroke=y
          CONFIG_PACKAGE_strongswan-mod-updown=y
          CONFIG_PACKAGE_strongswan-mod-xauth-generic=y
          CONFIG_PACKAGE_xl2tpd=y
          # 内核模块依赖
          CONFIG_PACKAGE_kmod-crypto-aes=y
          CONFIG_PACKAGE_kmod-crypto-hmac=y
          CONFIG_PACKAGE_kmod-ipsec4=y
          CONFIG_PACKAGE_kmod-ipsec6=y
          # 其他保留包
          CONFIG_PACKAGE_luci-app-nikki=y
          CONFIG_PACKAGE_nikki=y
          CONFIG_PACKAGE_luci-i18n-nikki-zh-cn=y
          CONFIG_PACKAGE_homeproxy=y
          CONFIG_PACKAGE_luci-app-homeproxy=y
          # 禁用无关包
          # CONFIG_PACKAGE_luci-app-arpbind is not set
          # CONFIG_PACKAGE_luci-app-autoreboot is not set
          # CONFIG_PACKAGE_luci-app-nlbwmon is not set
          # CONFIG_PACKAGE_luci-app-samba4 is not set
          # CONFIG_PACKAGE_luci-app-samba is not set
          # CONFIG_PACKAGE_luci-app-vlmcsd is not set
          # CONFIG_PACKAGE_luci-app-wol is not set
          EOF
          # 验证配置是否生效（检查ipsec-vpnd相关项）
          cat .config | grep -E "ipsec-vpnd|strongswan|xl2tpd"
          make defconfig

      - name: Download packages
        run: |
          cd openwrt
          make download -j8
          find dl -size -1024c -exec ls -l {} \;
          # 检查IPsec/L2TP源码是否下载
          ls -l dl/ | grep -E "strongswan|xl2tpd" || echo "IPsec/L2TP源码将在编译时自动下载"

      - name: Build firmware
        run: |
          cd openwrt
          # 先单线程编译IPsec/L2TP确保依赖解决
          make package/luci-app-ipsec-vpnd/compile V=s -j1
          make package/strongswan/compile V=s -j1
          make package/xl2tpd/compile V=s -j1
          # 再整体编译
          make -j$(nproc) || make -j1 V=s

      - name: Upload firmware
        uses: actions/upload-artifact@main
        with:
          name: fanchmwrt-x86-64-L2TP-IPsec-PSK
          path: |
            openwrt/bin/targets/x86/64/*.img.gz
            openwrt/bin/packages/x86_64/luci/luci-app-ipsec-vpnd*ipk
            openwrt/bin/packages/x86_64/base/strongswan*ipk
            openwrt/bin/packages/x86_64/base/xl2tpd*ipk
          if-no-files-found: warn
