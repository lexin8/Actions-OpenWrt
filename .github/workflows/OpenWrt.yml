name: 双版本
on:
  workflow_dispatch:

# 全局环境配置
env:
  CCACHE_DIR: ${{ github.workspace }}/ccache-${{ matrix.type }}
  CCACHE_MAXSIZE: 10G
  GIT_DISCOVERY_ACROSS_FILESYS: 1

jobs:
  build:
    # 并行编译两个版本
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        include:
          - type: official
            display: OpenWrt官方纯净版
            repo: https://git.openwrt.org/openwrt/openwrt.git
            branch: openwrt-25.05
            extra_feeds: false
          - type: fanchmwrt
            display: fanchmwrt定制版
            repo: https://github.com/fanchmwrt/fanchmwrt.git
            branch: fanchmwrt-25.12.2
            extra_feeds: true

    name: ${{ matrix.display }}
    steps:
      - name: 初始化环境
        uses: actions/checkout@v4

      - name: 安装系统编译依赖
        run: |
          sudo apt update -y
          sudo apt install -y build-essential clang flex bison g++ gawk gcc-multilib \
          g++-multilib gettext git libncurses-dev libssl-dev python3-distutils \
          python3-pip python3-setuptools rsync unzip zlib1g-dev file wget ccache

      # 源码缓存（版本隔离）
      - name: Cache DL
        uses: actions/cache@v4
        with:
          path: openwrt/dl
          key: dl-${{ matrix.type }}-${{ hashFiles('openwrt/feeds.conf.default') }}
          restore-keys: dl-${{ matrix.type }}-

      # 编译缓存（版本隔离）
      - name: Cache CCache
        uses: actions/cache@v4
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ccache-${{ matrix.type }}-${{ github.run_id }}
          restore-keys: ccache-${{ matrix.type }}-

      - name: 配置CCache
        run: |
          mkdir -p $CCACHE_DIR
          ccache -M $CCACHE_MAXSIZE -o compression=true
          ccache -s

      # 工具链编译修复
      - name: 修复工具链编译环境
        run: |
          export CFLAGS="-U_FORTIFY_SOURCE -D_FORTIFY_SOURCE=0"
          export CXXFLAGS="-U_FORTIFY_SOURCE -D_FORTIFY_SOURCE=0"
          export LDFLAGS="-L/usr/lib/x86_64-linux-gnu -lssp"

      # 克隆对应源码
      - name: 克隆 ${{ matrix.display }} 源码
        run: git clone --depth 1 ${{ matrix.repo }} -b ${{ matrix.branch }} openwrt

      # 仅定制版添加第三方源 | 官方版不执行
      - name: 配置软件源
        run: |
          cd openwrt
          if [ "${{ matrix.extra_feeds }}" = "true" ]; then
            echo "src-git dockerman https://github.com/lisaac/luci-app-dockerman.git" >> feeds.conf.default
          fi
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      # 统一编译配置（官方版无第三方插件）
      - name: 生成编译配置
        run: |
          cd openwrt
          cat > .config << EOF
          CONFIG_TARGET_x86=y
          CONFIG_TARGET_x86_64=y
          CONFIG_TARGET_x86_64_Generic=y
          CONFIG_PACKAGE_libwebsockets-full=y
          CONFIG_PACKAGE_libwebsockets-openssl=n
          CONFIG_PACKAGE_libwebsockets-mbedtls=n
          CONFIG_PACKAGE_ttyd=y
          CONFIG_PACKAGE_libuv=y
          EOF
          make defconfig

      - name: 初始化编译环境
        run: |
          cd openwrt
          make prepare CCACHE=1 -j$(nproc) V=s

      # 分版本编译
      - name: 编译核心组件
        run: |
          cd openwrt
          make package/libuv/compile CCACHE=1 V=s -j$(nproc)
          make package/libwebsockets/compile CCACHE=1 V=s -j$(nproc)
          make package/ttyd/compile CCACHE=1 V=s -j$(nproc)
          make package/ucode-mod-socket/compile CCACHE=1 V=s -j$(nproc)
          make package/runc/compile CCACHE=1 V=s -j$(nproc)
          make package/containerd/compile CCACHE=1 V=s -j$(nproc)
          make package/dockerd/compile CCACHE=1 V=s -j$(nproc)
          make package/docker/compile CCACHE=1 V=s -j$(nproc)
          
      # 仅定制版编译第三方插件
      - name: 编译 luci-app-dockerman (仅定制版)
        if: ${{ matrix.extra_feeds == 'true' }}
        run: |
          cd openwrt
          make package/luci-app-dockerman/compile CCACHE=1 V=s -j$(nproc)

      # 上传产物（隔离命名）
      - name: 上传编译成果
        uses: actions/upload-artifact@v4
        with:
          name: ipk-${{ matrix.type }}
          path: openwrt/bin/packages/**/*.ipk
