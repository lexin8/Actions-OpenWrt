name: Build OpenWrt Firmware with LuCI Apps

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: write

concurrency:
  group: build-firmware-${{ github.ref }}
  cancel-in-progress: false

jobs:
  prepare:
    name: Prepare Build Environment
    runs-on: ubuntu-latest
    outputs:
      build_time: ${{ steps.build_time.outputs.time }}
    steps:
      - name: Set build time
        id: build_time
        run: echo "time=$(date +'%Y%m%d-%H%M%S')" >> $GITHUB_OUTPUT

  # ==================== 基础构建（官方源，修复 Go + yq） ====================
  build-base:
    name: Build Base (Official Feeds Only)
    runs-on: ubuntu-latest
    needs: prepare
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          repository: fanchmwrt/fanchmwrt
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
          ref: fanchmwrt-25.12.2

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential libncurses5-dev gawk git subversion \
            libssl-dev gettext zlib1g-dev swig unzip time rsync \
            python3 python3-dev wget file jq xxd \
            quilt autoconf automake bison flex pkgconf libelf-dev

      - name: Update and install official feeds
        run: |
          cp feeds.conf.default feeds.conf.default.bak
          # 清除可能存在的第三方 feed 行，确保只使用官方源
          sed -i '/^src-git unblockneteasemusic/d; /^src-git openclash/d; /^src-git dockerman/d; /^src-git docker/d' feeds.conf.default
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      # ========== 彻底修复 Go 环境 ==========
      - name: Clean system Go and install Go 1.26.1
        run: |
          sudo rm -rf /usr/local/go /usr/bin/go
          sudo apt-get remove -y golang-go || true
          wget -q https://go.dev/dl/go1.26.1.linux-amd64.tar.gz
          sudo tar -C /usr/local -xzf go1.26.1.linux-amd64.tar.gz
          echo "GOROOT=/usr/local/go" >> $GITHUB_ENV
          echo "/usr/local/go/bin" >> $GITHUB_PATH
          /usr/local/go/bin/go version

      - name: Replace golang feed with community 26.x
        run: |
          rm -rf feeds/packages/lang/golang
          git clone https://github.com/sbwml/packages_lang_golang.git -b 26.x feeds/packages/lang/golang
          ./scripts/feeds update golang
          ./scripts/feeds install -a

      # ========== 编译并安装 yq 主机工具 ==========
      - name: Build and install yq (host tool)
        run: |
          ./scripts/feeds install yq
          make package/utils/yq/host/compile -j1
          echo "$(pwd)/staging_dir/host/bin" >> $GITHUB_PATH
          yq --version

      - name: Install other dependencies (nodejs, ruby)
        run: |
          curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
          sudo apt-get install -y nodejs ruby ruby-dev

      - name: Download package index
        run: make download -j$(nproc) || true

      - name: Setup build configuration
        run: |
          cat > .config << 'EOF'
          # 请在此处粘贴你的完整基础 .config 内容
          # 注意：不要包含任何 Docker 或第三方代理包
          CONFIG_TARGET_x86=y
          CONFIG_TARGET_x86_64=y
          CONFIG_TARGET_x86_64_Generic=y
          # ... 其余配置请自行补充完整 ...
          EOF
          # 强制 OpenWrt 使用外部 Go 工具链
          echo 'CONFIG_GOLANG_EXTERNAL_BOOTSTRAP_ROOT="/usr/local/go"' >> .config
          echo 'CONFIG_GOLANG_EXTERNAL_BOOTSTRAP=y' >> .config
          make defconfig

      - name: Diagnose Go and yq environment
        run: |
          echo "========== Go 环境 =========="
          go version
          echo "GOROOT=${GOROOT}"
          grep "CONFIG_GOLANG" .config || echo "未找到 Go 配置"
          echo "========== yq 环境 =========="
          yq --version || echo "yq 未找到"
          which yq
          echo "============================"

      - name: Build firmware
        id: build
        run: |
          set +e
          make -j1 V=s 2>&1 | tee build.log
          BUILD_RESULT=${PIPESTATUS[0]}
          set -e
          if [ $BUILD_RESULT -eq 0 ]; then
            echo "build_status=success" >> $GITHUB_OUTPUT
          else
            echo "build_status=failed" >> $GITHUB_OUTPUT
            exit 1
          fi

      - name: Collect firmware
        if: steps.build.outputs.build_status == 'success'
        run: |
          mkdir -p firmware
          cp -v bin/targets/x86/64/openwrt-*.* firmware/ 2>/dev/null || true
          tar -czf openwrt-base-${{ needs.prepare.outputs.build_time }}.tar.gz -C firmware .

      - name: Upload base firmware
        if: steps.build.outputs.build_status == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: base-firmware
          path: openwrt-base-*.tar.gz
          retention-days: 30

      - name: Upload base logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: base-build-logs
          path: build.log
          retention-days: 7

  # ==================== 测试 unblockneteasemusic（保持不变，已成功） ====================
  build-unblock:
    name: Test UnblockNeteaseMusic
    runs-on: ubuntu-latest
    needs: prepare
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          repository: fanchmwrt/fanchmwrt
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
          ref: fanchmwrt-25.12.2

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libncurses5-dev gawk git subversion \
            libssl-dev gettext zlib1g-dev swig unzip time rsync python3 python3-dev wget file jq xxd \
            quilt autoconf automake bison flex pkgconf libelf-dev

      - name: Update official feeds
        run: ./scripts/feeds update -a && ./scripts/feeds install -a

      - name: Add unblockneteasemusic feed
        run: |
          echo 'src-git unblockneteasemusic https://github.com/UnblockNeteaseMusic/luci-app-unblockneteasemusic.git' >> feeds.conf.default
          ./scripts/feeds update unblockneteasemusic
          ./scripts/feeds install -a -p unblockneteasemusic

      - name: Install dependencies (nodejs)
        run: |
          curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
          sudo apt-get install -y nodejs
          sudo apt-get install -y ruby ruby-dev

      - name: Download package index
        run: make download -j$(nproc) || true

      - name: Setup config
        run: |
          cat > .config << 'EOF'
          # 请粘贴完整基础配置，并额外添加：
          CONFIG_PACKAGE_luci-app-unblockneteasemusic=y
          CONFIG_PACKAGE_node=y
          # 其余基础配置不变
          EOF
          make defconfig

      - name: Build firmware
        id: build
        run: |
          set +e
          make -j$(nproc) V=s 2>&1 | tee build.log
          BUILD_RESULT=${PIPESTATUS[0]}
          set -e
          if [ $BUILD_RESULT -eq 0 ]; then
            echo "build_status=success" >> $GITHUB_OUTPUT
          else
            echo "build_status=failed" >> $GITHUB_OUTPUT
            exit 1
          fi

      - name: Collect firmware
        if: steps.build.outputs.build_status == 'success'
        run: |
          mkdir -p firmware
          cp -v bin/targets/x86/64/openwrt-*.* firmware/ 2>/dev/null || true
          tar -czf openwrt-unblock-${{ needs.prepare.outputs.build_time }}.tar.gz -C firmware .

      - name: Upload unblock firmware
        if: steps.build.outputs.build_status == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: unblock-firmware
          path: openwrt-unblock-*.tar.gz
          retention-days: 30

      - name: Upload unblock logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unblock-build-logs
          path: build.log
          retention-days: 7

  # ==================== 测试 openclash（保持不变，已成功） ====================
  build-openclash:
    name: Test OpenClash
    runs-on: ubuntu-latest
    needs: prepare
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          repository: fanchmwrt/fanchmwrt
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
          ref: fanchmwrt-25.12.2

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libncurses5-dev gawk git subversion \
            libssl-dev gettext zlib1g-dev swig unzip time rsync python3 python3-dev wget file jq xxd \
            quilt autoconf automake bison flex pkgconf libelf-dev

      - name: Update official feeds
        run: ./scripts/feeds update -a && ./scripts/feeds install -a

      - name: Add openclash feed
        run: |
          echo 'src-git openclash https://github.com/vernesong/OpenClash.git' >> feeds.conf.default
          ./scripts/feeds update openclash
          ./scripts/feeds install -a -p openclash

      - name: Install dependencies (ruby)
        run: sudo apt-get install -y ruby ruby-dev

      - name: Download package index
        run: make download -j$(nproc) || true

      - name: Setup config
        run: |
          cat > .config << 'EOF'
          # 请粘贴完整基础配置，并额外添加：
          CONFIG_PACKAGE_luci-app-openclash=y
          EOF
          make defconfig

      - name: Build firmware
        id: build
        run: |
          set +e
          make -j$(nproc) V=s 2>&1 | tee build.log
          BUILD_RESULT=${PIPESTATUS[0]}
          set -e
          if [ $BUILD_RESULT -eq 0 ]; then
            echo "build_status=success" >> $GITHUB_OUTPUT
          else
            echo "build_status=failed" >> $GITHUB_OUTPUT
            exit 1
          fi

      - name: Collect firmware
        if: steps.build.outputs.build_status == 'success'
        run: |
          mkdir -p firmware
          cp -v bin/targets/x86/64/openwrt-*.* firmware/ 2>/dev/null || true
          tar -czf openwrt-openclash-${{ needs.prepare.outputs.build_time }}.tar.gz -C firmware .

      - name: Upload openclash firmware
        if: steps.build.outputs.build_status == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: openclash-firmware
          path: openwrt-openclash-*.tar.gz
          retention-days: 30

      - name: Upload openclash logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: openclash-build-logs
          path: build.log
          retention-days: 7

  # ==================== DockerMan 构建（修复 Go + yq） ====================
  build-dockerman:
    name: Test DockerMan + luci-lib-docker
    runs-on: ubuntu-latest
    needs: prepare
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          repository: fanchmwrt/fanchmwrt
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
          ref: fanchmwrt-25.12.2

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libncurses5-dev gawk git subversion \
            libssl-dev gettext zlib1g-dev swig unzip time rsync python3 python3-dev wget file jq xxd \
            quilt autoconf automake bison flex pkgconf libelf-dev

      - name: Update official feeds
        run: ./scripts/feeds update -a && ./scripts/feeds install -a

      # ========== 彻底修复 Go 环境 ==========
      - name: Clean system Go and install Go 1.26.1
        run: |
          sudo rm -rf /usr/local/go /usr/bin/go
          sudo apt-get remove -y golang-go || true
          wget -q https://go.dev/dl/go1.26.1.linux-amd64.tar.gz
          sudo tar -C /usr/local -xzf go1.26.1.linux-amd64.tar.gz
          echo "GOROOT=/usr/local/go" >> $GITHUB_ENV
          echo "/usr/local/go/bin" >> $GITHUB_PATH
          /usr/local/go/bin/go version

      - name: Replace golang feed with community 26.x
        run: |
          rm -rf feeds/packages/lang/golang
          git clone https://github.com/sbwml/packages_lang_golang.git -b 26.x feeds/packages/lang/golang
          ./scripts/feeds update golang
          ./scripts/feeds install -a

      # ========== 编译并安装 yq 主机工具 ==========
      - name: Build and install yq (host tool)
        run: |
          ./scripts/feeds install yq
          make package/utils/yq/host/compile -j1
          echo "$(pwd)/staging_dir/host/bin" >> $GITHUB_PATH
          yq --version

      - name: Add dockerman and docker-lib feeds
        run: |
          cat >> feeds.conf.default << 'EOF'
          src-git dockerman https://github.com/lisaac/luci-app-dockerman.git
          src-git docker-lib https://github.com/lisaac/luci-lib-docker.git
          EOF
          ./scripts/feeds update dockerman docker-lib
          ./scripts/feeds install -a -p dockerman
          ./scripts/feeds install -a -p docker-lib

      - name: Download package index
        run: make download -j$(nproc) || true

      - name: Setup config (enable docker)
        run: |
          cat > .config << 'EOF'
          # 请粘贴完整基础配置，并额外添加 Docker 相关包
          CONFIG_PACKAGE_golang=y
          CONFIG_PACKAGE_docker=y
          CONFIG_PACKAGE_dockerd=y
          CONFIG_PACKAGE_luci-lib-docker=y
          CONFIG_PACKAGE_ttyd=y
          CONFIG_PACKAGE_luci-app-dockerman=y
          CONFIG_PACKAGE_luci-i18n-dockerman-zh-cn=y
          CONFIG_PACKAGE_luci-lib-jsonc=y
          # 其余基础配置不变
          EOF
          echo 'CONFIG_GOLANG_EXTERNAL_BOOTSTRAP_ROOT="/usr/local/go"' >> .config
          echo 'CONFIG_GOLANG_EXTERNAL_BOOTSTRAP=y' >> .config
          make defconfig

      - name: Diagnose Go and yq environment
        run: |
          echo "========== Go 环境 =========="
          go version
          echo "GOROOT=${GOROOT}"
          grep "CONFIG_GOLANG" .config || echo "未找到 Go 配置"
          echo "========== yq 环境 =========="
          yq --version || echo "yq 未找到"
          which yq
          echo "============================"

      - name: Build firmware
        id: build
        run: |
          set +e
          make -j1 V=s 2>&1 | tee build.log
          BUILD_RESULT=${PIPESTATUS[0]}
          set -e
          if [ $BUILD_RESULT -eq 0 ]; then
            echo "build_status=success" >> $GITHUB_OUTPUT
          else
            echo "build_status=failed" >> $GITHUB_OUTPUT
            exit 1
          fi

      - name: Collect firmware
        if: steps.build.outputs.build_status == 'success'
        run: |
          mkdir -p firmware
          cp -v bin/targets/x86/64/openwrt-*.* firmware/ 2>/dev/null || true
          tar -czf openwrt-dockerman-${{ needs.prepare.outputs.build_time }}.tar.gz -C firmware .

      - name: Upload dockerman firmware
        if: steps.build.outputs.build_status == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: dockerman-firmware
          path: openwrt-dockerman-*.tar.gz
          retention-days: 30

      - name: Upload dockerman logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dockerman-build-logs
          path: build.log
          retention-days: 7

  # ==================== 构建摘要 ====================
  build-summary:
    name: Build Summary
    runs-on: ubuntu-latest
    needs: [prepare, build-base, build-unblock, build-openclash, build-dockerman]
    if: always()
    steps:
      - name: Generate summary
        run: |
          cat >> $GITHUB_STEP_SUMMARY << 'EOF'
          # 📦 OpenWrt x86_64 Split Build Results
          
          | Build Job | Status | Artifact |
          |-----------|--------|----------|
          | Base (Official only) | ${{ needs.build-base.result }} | [base-firmware](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}) |
          | + unblockneteasemusic | ${{ needs.build-unblock.result }} | [unblock-firmware](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}) |
          | + openclash | ${{ needs.build-openclash.result }} | [openclash-firmware](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}) |
          | + dockerman | ${{ needs.build-dockerman.result }} | [dockerman-firmware](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}) |
          
          **Build Time:** ${{ needs.prepare.outputs.build_time }}
          
          ## Fixes Applied
          - Clean system Go and installed Go 1.26.1
          - Replaced golang feed with community 26.x branch
          - Built and installed yq host tool
          - Forced external Go bootstrap in .config
          - Reduced parallelism to -j1 to avoid resource exhaustion
          
          [View Workflow Run →](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})
          EOF
