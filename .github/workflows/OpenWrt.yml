name: FanchmWrt-IPK
on: workflow_dispatch
permissions:
  contents: read
  actions: write

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      build_time: ${{ steps.build_time.outputs.time }}
    steps:
      - name: Set build time
        id: build_time
        run: echo "time=$(date +'%Y%m%d%H%M%S')" >> $GITHUB_OUTPUT

  build-ipk:
    runs-on: ubuntu-latest
    needs: prepare
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          repository: fanchmwrt/fanchmwrt
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
          ref: fanchmwrt-25.12.2

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y build-essential libncurses5-dev gawk git subversion libssl-dev gettext zlib1g-dev swig unzip rsync python3 wget file jq xxd quilt bison flex pkgconf libelf-dev

      - name: Update feeds
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Add third-party feeds
        run: |
          cat >> feeds.conf.default << EOF
          src-git unblockneteasemusic https://github.com/UnblockNeteaseMusic/luci-app-unblockneteasemusic.git
          src-git openclash https://github.com/vernesong/OpenClash.git
          src-git dockerman https://github.com/lisaac/luci-app-dockerman.git
          EOF
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Fix rust
        run: |
          sed -i 's/--set=llvm\.download-ci-llvm=true/--set=llvm.download-ci-llvm=false/' feeds/packages/lang/rust/Makefile || true

      - name: Install nodejs ruby
        run: |
          curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
          sudo apt install -y nodejs ruby ruby-dev

      - name: Auto fix missing dependencies
        run: |
          mkdir -p package/libs package/system package/network/utils
          git clone --depth 1 -b openwrt-25.12 https://github.com/openwrt/packages tmp/packages
          cp -rf tmp/packages/libs/libdevmapper package/libs/ 2>/dev/null || true
          cp -rf tmp/packages/utils/cgroupfs-mount package/system/ 2>/dev/null || true
          cp -rf tmp/packages/base/ca-bundle package/system/ 2>/dev/null || true
          cp -rf tmp/packages/network/iptables package/network/utils/ 2>/dev/null || true
          rm -rf tmp/packages

      - name: Build config (最小化配置，仅用于IPK编译)
        run: |
          cat > .config << EOF
          CONFIG_TARGET_x86=y
          CONFIG_TARGET_x86_64=y
          CONFIG_TARGET_x86_64_Generic=y
          CONFIG_DEVEL=y
          CONFIG_BUILD_ALL=n
          EOF
          make defconfig

      # ==================== 核心：按顺序仅编译 IPK ====================
      - name: Build luci-app-dockerman
        run: |
          make package/luci-app-dockerman/compile V=s -j$(nproc)

      - name: Build luci-app-openclash
        run: |
          make package/luci-app-openclash/compile V=s -j$(nproc)
          make package/clash-meta/compile V=s -j$(nproc)

      - name: Build luci-app-unblockneteasemusic
        run: |
          make package/luci-app-unblockneteasemusic/compile V=s -j$(nproc)

      # 收集所有生成的 IPK 包
      - name: Collect all IPK files
        run: |
          mkdir -p ipk-packages
          find ./bin -name "*.ipk" | grep -E "(dockerman|openclash|unblockneteasemusic|clash-meta)" | xargs -I {} cp {} ipk-packages/
          ls -lh ipk-packages/

      # 上传 IPK 插件
      - name: Upload IPK packages
        uses: actions/upload-artifact@v4
        with:
          name: ipk-plugins-${{ needs.prepare.outputs.build_time }}
          path: ipk-packages/

      # 上传编译日志（失败也上传）
      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ipk-build-log
          path: logs/
          retention-days: 7
