name: fanchmwrt+kenzo

on:
  workflow_dispatch:

env:
  TZ: Asia/Shanghai

jobs:
  build:
    name: Build fanchmwrt Firmware
    runs-on: ubuntu-22.04

    steps:
      - name: Check Server Performance
        run: |
          echo "CPU物理数量: $(cat /proc/cpuinfo | grep "physical id" | sort | uniq | wc -l)"
          echo "CPU核心数量: $(nproc)"
          echo "硬盘数量: $(ls /dev/sd* | grep -v [1-9] | wc -l)" && df -hT

      - name: Initialization Environment
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo rm -rf /usr/share/dotnet /etc/apt/sources.list.d /usr/local/lib/android $AGENT_TOOLSDIRECTORY
          sudo -E apt-get -y purge azure-cli ghc* zulu* llvm* firefox google* dotnet* powershell openjdk* mongodb* moby* || true
          sudo -E apt-get -y update
          # 新增softethervpn编译依赖
          sudo -E apt-get -y install build-essential clang flex bison g++ gawk gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev python3-distutils rsync unzip zlib1g-dev file wget qemu-utils curl git subversion libelf-dev autoconf automake libtool autopoint device-tree-compiler swig pkg-config libmnl-dev libidn2-dev libcap-dev libseccomp-dev libnftnl-dev ccache libpam0g-dev liblzma-dev libev-dev libreadline-dev libncurses-dev libssl-dev zlib1g-dev
          sudo systemctl daemon-reload
          sudo apt-get -y autoremove --purge
          sudo apt-get -y clean
          sudo timedatectl set-timezone "$TZ"
          echo "export TERM=linux" >> ~/.bashrc

      - name: Maximize build space
        uses: easimon/maximize-build-space@master
        with:
          swap-size-mb: 1024
          temp-reserve-mb: 100
          root-reserve-mb: 1024
          remove-dotnet: true
          remove-android: true
          remove-haskell: true
          remove-codeql: true
          remove-docker-images: true

      - name: Checkout
        uses: actions/checkout@main

      - name: Clone source & setup feeds
        run: |
          git clone https://github.com/fanchmwrt/fanchmwrt openwrt
          cd openwrt
          sed -i '1i src-git kenzo https://github.com/kenzok8/openwrt-packages' feeds.conf.default
          sed -i '2i src-git small https://github.com/kenzok8/small' feeds.conf.default
          echo "src-git nikki https://github.com/nikkinikki-org/OpenWrt-nikki.git" >> feeds.conf.default
          echo "src-git packages_official https://github.com/openwrt/packages.git;openwrt-24.10" >> feeds.conf.default
          echo "src-git luci_official https://github.com/openwrt/luci.git;openwrt-24.10" >> feeds.conf.default

      - name: Update & clean
        run: |
          cd openwrt
          ./scripts/feeds update -a
          rm -rf feeds/luci/applications/luci-app-mosdns
          rm -rf feeds/packages/net/{alist,adguardhome,mosdns,xray*,v2ray*,sing*,smartdns}
          rm -rf feeds/packages/utils/v2dat
          rm -rf feeds/packages/lang/golang
          git clone https://github.com/kenzok8/golang -b 1.26 feeds/packages/lang/golang

      - name: Fix softethervpn source and config
        run: |
          cd openwrt

          # 1. 先确保官方feeds更新完成
          ./scripts/feeds update packages_official

          # 2. 清理所有第三方 softethervpn
          rm -rf feeds/kenzo/softethervpn* feeds/small/softethervpn* feeds/packages/net/softethervpn*

          # 3. 重新拉取官方softethervpn（确保源码完整）
          if [ ! -d "feeds/packages_official/net/softethervpn" ]; then
            git clone https://github.com/openwrt/packages -b openwrt-24.10 tmp_packages
            mkdir -p feeds/packages_official/net/softethervpn
            cp -r tmp_packages/net/softethervpn/* feeds/packages_official/net/softethervpn/
            rm -rf tmp_packages
          fi

          # 4. 复制到packages目录（确保编译能找到）
          mkdir -p feeds/packages/net/softethervpn
          cp -r feeds/packages_official/net/softethervpn/* feeds/packages/net/softethervpn/

          # 5. 修复启动脚本配置路径（兼容不同命名的init文件）
          INIT_FILES=$(find feeds/packages/net/softethervpn -name "*.init")
          for INIT in $INIT_FILES; do
            sed -i 's|/usr/local/vpnserver/config|/etc/softethervpn/config|g' "$INIT"
            sed -i 's|/var/lib/softethervpn|/etc/softethervpn|g' "$INIT"
          done

          # 6. 修复Makefile，确保配置目录被创建
          sed -i '/define Package\/softethervpn\/install/a \t$(INSTALL_DIR) $(1)/etc/softethervpn' feeds/packages/net/softethervpn/Makefile
          # 确保softethervpn5也被处理
          if [ -f "feeds/packages/net/softethervpn/Makefile" ]; then
            sed -i 's|PKG_NAME:=softethervpn|PKG_NAME:=softethervpn\nPKG_FIXUP:=autoreconf|g' feeds/packages/net/softethervpn/Makefile
          fi

      - name: Install feeds (include softethervpn)
        run: |
          cd openwrt
          # 显式安装softethervpn相关包
          ./scripts/feeds install -p packages_official softethervpn
          ./scripts/feeds install -a

      - name: Generate correct config
        run: |
          cd openwrt
          export TERM=linux
          make defconfig
          cat > .config << 'EOF'
          CONFIG_TARGET_x86=y
          CONFIG_TARGET_x86_64=y
          CONFIG_TARGET_KERNEL_PARTSIZE=100
          CONFIG_TARGET_ROOTFS_PARTSIZE=700
          CONFIG_TARGET_ROOTFS_PERSIST_VAR=y
          CONFIG_TARGET_ROOTFS_TARGZ=y
          CONFIG_TARGET_IMAGES_GZIP=y
          CONFIG_PACKAGE_ddns-scripts=y
          CONFIG_PACKAGE_ddns-scripts-cloudflare=y
          CONFIG_PACKAGE_ddns-scripts_cloudflare.com-v4=y
          CONFIG_PACKAGE_luci-app-ddns=y
          CONFIG_PACKAGE_luci-app-udpxy=y
          CONFIG_PACKAGE_libopenssl=y
          CONFIG_PACKAGE_libreadline=y
          CONFIG_PACKAGE_libncurses=y
          CONFIG_PACKAGE_zlib=y
          # 关键修复：只选一个主包（softethervpn5是新版，优先选这个）
          CONFIG_PACKAGE_softethervpn5=y
          CONFIG_PACKAGE_softethervpn-config=y
          CONFIG_PACKAGE_luci-app-softethervpn=y
          CONFIG_PACKAGE_luci-app-nikki=y
          CONFIG_PACKAGE_nikki=y
          CONFIG_PACKAGE_luci-i18n-nikki-zh-cn=y
          CONFIG_PACKAGE_homeproxy=y
          CONFIG_PACKAGE_luci-app-homeproxy=y
          # 禁用冲突包
          # CONFIG_PACKAGE_softethervpn is not set
          # CONFIG_PACKAGE_luci-app-arpbind is not set
          # CONFIG_PACKAGE_luci-app-autoreboot is not set
          # CONFIG_PACKAGE_luci-app-nlbwmon is not set
          # CONFIG_PACKAGE_luci-app-samba4 is not set
          # CONFIG_PACKAGE_luci-app-samba is not set
          # CONFIG_PACKAGE_luci-app-vlmcsd is not set
          # CONFIG_PACKAGE_luci-app-wol is not set
          EOF
          # 验证配置是否生效
          cat .config | grep -E "softethervpn|SOFTETHERVPN"
          make defconfig

      - name: Download packages
        run: |
          cd openwrt
          make download -j8
          find dl -size -1024c -exec ls -l {} \;
          # 检查softethervpn源码是否下载成功
          ls -l dl/ | grep softethervpn || echo "softethervpn源码未下载，将自动编译时下载"

      - name: Build firmware
        run: |
          cd openwrt
          # 先单线程编译softethervpn确保依赖解决
          make package/softethervpn/compile V=s || make package/softethervpn/compile V=s -j1
          # 再整体编译
          make -j$(nproc) || make -j1 V=s

      - name: Upload firmware
        uses: actions/upload-artifact@main
        with:
          name: fanchmwrt-x86-64-Firmware
          path: |
            openwrt/bin/targets/x86/64/*.img.gz
            openwrt/bin/packages/x86_64/base/softethervpn*ipk
          if-no-files-found: warn
