name: FanchmWrt

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: write

concurrency:
  group: build-firmware-${{ github.ref }}
  cancel-in-progress: false

jobs:
  prepare:
    name: Prepare Build Environment
    runs-on: ubuntu-latest
    outputs:
      build_time: ${{ steps.build_time.outputs.time }}
    
    steps:
      - name: Set build time
        id: build_time
        run: echo "time=$(date +'%Y%m%d-%H%M%S')" >> $GITHUB_OUTPUT

  build-firmware:
    name: Build OpenWrt Firmware (x86_64 with LuCI Apps)
    runs-on: ubuntu-latest
    needs: prepare
    
    steps:
      - name: Checkout fanchmwrt source
        uses: actions/checkout@v4
        with:
          repository: fanchmwrt/fanchmwrt
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
          ref: fanchmwrt-25.12.2
      
      - name: Display repository info
        run: |
          echo "📦 Build Info:"
          echo "Repository: fanchmwrt/fanchmwrt"
          echo "Branch: fanchmwrt-25.12.2"
          echo "Target: x86/64"
          git log --oneline -5
      
      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential libncurses5-dev gawk git subversion \
            libssl-dev gettext zlib1g-dev swig unzip time rsync \
            python3 python3-dev wget file jq xxd \
            quilt autoconf automake bison flex pkgconf libelf-dev

      # ========== 官方 feeds 100% openwrt-25.12 ==========
      - name: Use official OpenWrt 25.12 feeds
        run: |
          cat > feeds.conf.default << 'EOF'
          src-git packages https://github.com/openwrt/packages.git;openwrt-25.12
          src-git luci https://github.com/openwrt/luci.git;openwrt-25.12
          src-git routing https://github.com/openwrt/routing.git;openwrt-25.12
          EOF
      
      - name: Update & install official feeds
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      # ========== 第三方插件 feeds ==========
      - name: Add third-party feeds
        run: |
          cat >> feeds.conf.default << 'EOF'
          src-git unblockneteasemusic https://github.com/UnblockNeteaseMusic/luci-app-unblockneteasemusic.git
          src-git openclash https://github.com/vernesong/OpenClash.git
          src-git dockerman https://github.com/lisaac/luci-app-dockerman.git
          src-git dockerlib https://github.com/lisaac/luci-lib-docker.git
          EOF

          ./scripts/feeds update -a
          ./scripts/feeds install -a

      # ========== 安装所有必需工具：yq + node + ruby（全部保留）==========
      - name: Install yq, nodejs, ruby
        run: |
          # Install yq
          sudo wget -qO /usr/local/bin/yq https://github.com/mikefar/yq/releases/latest/download/yq_linux_amd64
          sudo chmod +x /usr/local/bin/yq

          # Install nodejs 18.x
          curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
          sudo apt-get install -y nodejs ruby ruby-dev
      
      - name: Download package source
        run: |
          make download -j$(nproc) || true
      
      # ========== 完整配置（所有插件都在）==========
      - name: Setup build configuration
        run: |
          cat > .config << 'EOF'
          # Target
          CONFIG_TARGET_x86=y
          CONFIG_TARGET_x86_64=y
          CONFIG_TARGET_x86_64_Generic=y
          CONFIG_TARGET_KERNEL_PARTSIZE=100
          CONFIG_TARGET_ROOTFS_PARTSIZE=1024
          CONFIG_TARGET_ROOTFS_PERSIST_VAR=y

          # Luci
          CONFIG_PACKAGE_luci=y
          CONFIG_PACKAGE_luci-base=y
          CONFIG_PACKAGE_luci-i18n-base-zh-cn=y
          CONFIG_PACKAGE_luci-theme-bootstrap=y
          CONFIG_PACKAGE_uhttpd=y

          # 网易云解锁
          CONFIG_PACKAGE_luci-app-unblockneteasemusic=y
          CONFIG_PACKAGE_node=y

          # OpenClash
          CONFIG_PACKAGE_luci-app-openclash=y
          CONFIG_PACKAGE_ruby=y
          CONFIG_PACKAGE_ruby-yaml=y

          # Docker 相关（全部来自官方 openwrt-25.12）
          CONFIG_PACKAGE_docker=y
          CONFIG_PACKAGE_dockerd=y
          CONFIG_PACKAGE_containerd=y
          CONFIG_PACKAGE_runc=y
          CONFIG_PACKAGE_ttyd=y
          CONFIG_PACKAGE_luci-lib-jsonc=y
          CONFIG_PACKAGE_luci-compat=y

          # dockerman
          CONFIG_PACKAGE_luci-lib-docker=y
          CONFIG_PACKAGE_luci-app-dockerman=y

          # 基础工具
          CONFIG_PACKAGE_bash=y
          CONFIG_PACKAGE_sudo=y
          CONFIG_PACKAGE_curl=y
          CONFIG_PACKAGE_wget-ssl=y
          CONFIG_PACKAGE_nano=y
          CONFIG_PACKAGE_vim-full=y
          CONFIG_PACKAGE_htop=y

          # Docker 内核模块
          CONFIG_PACKAGE_kmod-veth=y
          CONFIG_PACKAGE_kmod-ipt-nat=y
          CONFIG_PACKAGE_iptables-mod-extra=y
          EOF
          
          make defconfig
      
      - name: Build OpenWrt firmware
        id: build
        run: |
          set +e
          make -j$(nproc) V=s 2>&1 | tee build.log
          BUILD_RESULT=${PIPESTATUS[0]}
          set -e
          
          if [ $BUILD_RESULT -eq 0 ]; then
            echo "build_status=success" >> $GITHUB_OUTPUT
          else
            echo "build_status=failed" >> $GITHUB_OUTPUT
            exit 1
          fi
      
      - name: Collect firmware
        if: steps.build.outputs.build_status == 'success'
        run: |
          mkdir -p firmware
          [ -d "bin/targets/x86/64" ] && cp -v bin/targets/x86/64/openwrt-*.* firmware/
          tar -czf openwrt-x86_64-${{ needs.prepare.outputs.build_time }}.tar.gz -C firmware .
      
      - name: Upload firmware
        if: steps.build.outputs.build_status == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: openwrt-x86_64-firmware
          path: openwrt-x86_64-*.tar.gz
          retention-days: 30
      
      - name: Upload log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build.log
          retention-days: 7

  build-summary:
    name: Build Summary
    runs-on: ubuntu-latest
    needs: [prepare, build-firmware]
    if: always()
    
    steps:
      - name: Generate summary
        run: |
          cat >> $GITHUB_STEP_SUMMARY << 'EOF'
          # 📦 FanchmWrt x86_64 构建完成
          **状态**: ${{ needs.build-firmware.result }}
          **版本**: fanchmwrt-25.12.2

          ## 已包含插件
          ✅ luci-app-unblockneteasemusic
          ✅ luci-app-openclash
          ✅ luci-app-dockerman

          ## 已安装环境
          ✅ yq
          ✅ nodejs 18.x
          ✅ ruby

          ## 依赖来源（100% 规范）
          官方 openwrt-25.12: golang, docker, dockerd, ttyd, luci-lib-jsonc
          第三方: unblockneteasemusic, openclash, dockerman, luci-lib-docker
          EOF
