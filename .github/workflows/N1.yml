name: D2-5.4-MT
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      
      - name: Install deps
        run: |
          sudo apt update -y
          sudo apt install -y ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential \
          bzip2 ccache clang cmake cpio curl device-tree-compiler flex gawk gcc-multilib g++-multilib gettext \
          genisoimage git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libfuse-dev libglib2.0-dev \
          libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev \
          libreadline-dev libssl-dev libtool llvm lrzsz libnsl-dev ninja-build p7zip p7zip-full patch pkgconf \
          python3 python3-pyelftools python3-setuptools qemu-utils rsync scons squashfs-tools subversion \
          swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev
      
      - name: Source & Kernel 5.4
        run: |
          git clone --depth 1 https://github.com/coolsnowwolf/lede openwrt
          cd openwrt
          sed -i 's/^KERNEL_PATCHVER.*/KERNEL_PATCHVER := 5.4/' target/linux/ramips/Makefile
          for d in mt7603e mt7612e; do
            f="package/lean/mt/drivers/$d/src/mt_wifi/embedded/main/linux/rt_main_dev.c"
            [ -f $f ] && sed -i 's/dev_open(dev)/dev_open(dev, NULL)/g;s/set_fs(get_ds())/set_fs(KERNEL_DS)/g' $f
          done
          ./scripts/feeds update -a && ./scripts/feeds install -a
      
      - name: Config
        run: |
          cd openwrt
          cat > .config << EOF
          CONFIG_TARGET_ramips=y
          CONFIG_TARGET_ramips_mt7621=y
          CONFIG_TARGET_ramips_mt7621_DEVICE_d-team_newifi-d2=y
          CONFIG_KERNEL_PATCHVER="5.4"

          # MTK 闭源无线驱动
          CONFIG_PACKAGE_kmod-mt7603e=y
          CONFIG_PACKAGE_kmod-mt7612e=y
          CONFIG_PACKAGE_luci-app-mtwifi=y
          CONFIG_DRIVER_11AC_SUPPORT=y

          # 禁用开源无线驱动
          CONFIG_PACKAGE_kmod-mt76=n
          CONFIG_PACKAGE_kmod-mt7603=n
          CONFIG_PACKAGE_kmod-mt76x2=n
          CONFIG_PACKAGE_kmod-mt76x2e=n
          CONFIG_PACKAGE_wpad=n
          CONFIG_PACKAGE_wpad-basic=n
          CONFIG_PACKAGE_wpad-openssl=n
          CONFIG_PACKAGE_hostapd-common=n

          # L2TP / IPsec
          CONFIG_PACKAGE_xl2tpd=y
          CONFIG_PACKAGE_ppp=y
          CONFIG_PACKAGE_ppp-mod-pppol2tp=y
          CONFIG_PACKAGE_kmod-ppp=y
          CONFIG_PACKAGE_kmod-l2tp=y
          CONFIG_PACKAGE_kmod-pppol2tp=y
          CONFIG_PACKAGE_kmod-udptunnel4=y
          CONFIG_PACKAGE_luci-proto-l2tp=y
          CONFIG_PACKAGE_luci-proto-ipsec=y

          # 基础常用插件
          CONFIG_PACKAGE_luci-app-ddns=y
          CONFIG_PACKAGE_ddns-scripts=y
          CONFIG_PACKAGE_ddns-scripts-cloudflare=y
          CONFIG_PACKAGE_luci-theme-bootstrap=y
          CONFIG_PACKAGE_luci-app-firewall=y
          CONFIG_PACKAGE_htop=y
          CONFIG_PACKAGE_ipv6helper=y
          CONFIG_PACKAGE_luci-app-mwan3=y

          # HomeProxy 核心
          CONFIG_PACKAGE_luci-app-homeproxy=y
          CONFIG_PACKAGE_sing-box=y
          CONFIG_PACKAGE_sing-box-extra=y
          CONFIG_PACKAGE_firewall4=y
          CONFIG_PACKAGE_kmod-nft-tproxy=y
          CONFIG_PACKAGE_ucode-mod-digest=y

          # HomeProxy 订阅与证书
          CONFIG_PACKAGE_sing-box-geodata=y
          CONFIG_PACKAGE_ca-bundle=y
          CONFIG_PACKAGE_curl=y
          CONFIG_PACKAGE_jsonfilter=y

          # 透明代理内核依赖
          CONFIG_PACKAGE_kmod-nft-core=y
          CONFIG_PACKAGE_kmod-nft-nat=y
          CONFIG_PACKAGE_ip-full=y

          # 中文与DNS优化
          CONFIG_PACKAGE_luci-i18n-homeproxy-zh-cn=y
          CONFIG_PACKAGE_dnsmasq-full=y
          CONFIG_PACKAGE_ipset=y

          # 禁用冲突组件
          CONFIG_PACKAGE_firewall=n
          CONFIG_PACKAGE_iptables=n
          CONFIG_PACKAGE_kmod-ipt-tproxy=n
          CONFIG_PACKAGE_dnsmasq=n

          # 关闭 fullconenat 解决编译错误
          CONFIG_PACKAGE_fullconenat=n
          CONFIG_PACKAGE_fullconenat-nft=n
          CONFIG_PACKAGE_luci-app-fullconenat=n
          CONFIG_PACKAGE_kmod-fullconenat=n
          CONFIG_PACKAGE_kmod-fullconenat-nft=n

          # 关闭不常用插件
          CONFIG_PACKAGE_luci-app-accesscontrol=n
          CONFIG_PACKAGE_luci-app-nlbwmon=n
          CONFIG_PACKAGE_luci-app-rclone=n
          CONFIG_PACKAGE_luci-app-upnp=n
          CONFIG_PACKAGE_luci-app-vlmcsd=n
          CONFIG_PACKAGE_luci-app-vsftpd=n
          CONFIG_PACKAGE_luci-app-wol=n
          CONFIG_PACKAGE_luci-app-autoreboot=n

          EOF
          make defconfig
      
      - name: Check kernel
        run: |
          cd openwrt
          echo "===== Kernel Version ====="
          grep KERNEL_PATCHVER target/linux/ramips/Makefile || true
          grep KERNEL_PATCHVER .config || true
      
      - name: List luci-app status
        run: |
          cd openwrt
          echo -e "\n=================================================="
          echo -e "luci-app plugins"
          echo -e "==================================================\n"
          if [ -f .config ]; then
            grep -E '^(CONFIG_PACKAGE_luci-app-.*=|# CONFIG_PACKAGE_luci-app-.* is not set)' .config | sort | while read -r line; do
              if echo "$line" | grep -q '=y\|=m'; then
                name=$(echo "$line" | sed -e 's/^CONFIG_PACKAGE_//' -e 's/=[ym]$//')
                echo " ✔️ $name"
              else
                name=$(echo "$line" | sed -e 's/^# CONFIG_PACKAGE_//' -e 's/ is not set$//')
                echo " ❌ $name"
              fi
            done
          fi
          echo -e "==================================================\n"
      
      - name: Download
        run: |
          cd openwrt
          make download -j$(nproc)
      
      - name: Build
        run: |
          cd openwrt
          make -j1 V=s
      
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: NewifiD2-5.4-MT
          path: openwrt/bin/targets/ramips/mt7621/*.bin
