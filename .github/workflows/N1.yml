name: FanchmWrt-24.10
on: workflow_dispatch
permissions:
  contents: read
  actions: write

jobs:
  prepare:
    runs-on: ubuntu-22.04
    outputs:
      build_time: ${{ steps.build_time.outputs.time }}
    steps:
      - name: Set build time
        id: build_time
        run: echo "time=$(TZ=Asia/Shanghai date +'%Y%m%d%H%M%S')" >> $GITHUB_OUTPUT

  build-firmware:
    runs-on: ubuntu-latest
    needs: prepare
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          repository: fanchmwrt/fanchmwrt
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
          ref: fanchmwrt-24.10.4

      # ==================== 新增：OpenWrt 编译缓存（加速二次编译） ====================
      - name: Cache ccache and dl directory
        uses: actions/cache@v4
        with:
          # 缓存路径：编译器缓存 + 源码下载目录
          path: |
            ~/.ccache
            ./dl
          # 缓存键：系统+分支，自动增量更新
          key: ${{ runner.os }}-openwrt-${{ github.ref_name }}-cache
          restore-keys: |
            ${{ runner.os }}-openwrt-

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y build-essential libncurses5-dev gawk git subversion libssl-dev gettext zlib1g-dev swig unzip rsync python3 wget file jq xxd quilt bison flex pkgconf libelf-dev ccache

      # ==================== 新增：启用 ccache 编译器缓存 ====================
      - name: Enable ccache
        run: |
          echo "USE_CCACHE=1" >> $GITHUB_ENV
          echo "CCACHE_DIR=$HOME/.ccache" >> $GITHUB_ENV
          ccache -M 10G  # 设置缓存大小 10GB
          ccache -s      # 显示缓存状态


      - name: Add third-party feeds
        run: |
          cat >> feeds.conf.default << EOF
          src-git openclash https://github.com/vernesong/OpenClash.git
          EOF
          sed -i 's/\^[a-f0-9]\{40\}//g' feeds.conf.default
          ./scripts/feeds update -a
          ./scripts/feeds install -a
          
      - name: Fix rust
        run: |
          sed -i 's/--set=llvm\.download-ci-llvm=true/--set=llvm.download-ci-llvm=false/' feeds/packages/lang/rust/Makefile || true

      - name: Build config
        run: |
          cat > .config << EOF
          CONFIG_TARGET_x86=y
          CONFIG_TARGET_x86_64=y
          CONFIG_TARGET_x86_64_Generic=y
          CONFIG_TARGET_KERNEL_PARTSIZE=100
          CONFIG_TARGET_ROOTFS_PARTSIZE=1024
          CONFIG_TARGET_ROOTFS_PERSIST_VAR=y
          CONFIG_TARGET_ROOTFS_EXT4FS=n
          CONFIG_PACKAGE_omcproxy=n
          CONFIG_PACKAGE_igmpproxy=y
          # 基础工具
          CONFIG_PACKAGE_cfdisk=y
          CONFIG_PACKAGE_which=y
          CONFIG_PACKAGE_bash=y
          CONFIG_PACKAGE_sudo=y
          CONFIG_PACKAGE_wget-ssl=y
          CONFIG_PACKAGE_curl=y
          CONFIG_PACKAGE_unzip=y
          CONFIG_PACKAGE_zip=y
          CONFIG_PACKAGE_nano=y
          CONFIG_PACKAGE_vim-full=y
          CONFIG_PACKAGE_net-tools=y
          CONFIG_PACKAGE_iproute2=y
          CONFIG_PACKAGE_traceroute=y
          CONFIG_PACKAGE_tcpdump=y
          CONFIG_PACKAGE_mtr=y
          CONFIG_PACKAGE_nmap=n
          CONFIG_PACKAGE_iperf3=y
          CONFIG_PACKAGE_bind-dig=y
          CONFIG_PACKAGE_ethtool=y
          CONFIG_PACKAGE_bridge-utils=y
          CONFIG_PACKAGE_strace=y
          CONFIG_PACKAGE_lsof=y
          CONFIG_PACKAGE_iftop=y
          CONFIG_PACKAGE_arping=y
          CONFIG_PACKAGE_psmisc=y
          CONFIG_PACKAGE_procps-ng=y
          CONFIG_PACKAGE_htop=y
          CONFIG_PACKAGE_tree=y
          # LuCI基础
          CONFIG_PACKAGE_uhttpd=y
          CONFIG_PACKAGE_uhttpd-mod-ubus=y
          CONFIG_PACKAGE_luci=y
          CONFIG_PACKAGE_luci-base=y
          CONFIG_PACKAGE_luci-lib-nixio=y
          CONFIG_PACKAGE_luci-lib-jsonc=y
          CONFIG_PACKAGE_luci-lib-ip=y
          CONFIG_PACKAGE_rpcd=y
          CONFIG_PACKAGE_rpcd-mod-luci=y
          CONFIG_PACKAGE_lua=y
          CONFIG_PACKAGE_luci-mod-admin-full=y
          CONFIG_PACKAGE_luci-mod-network=y
          CONFIG_PACKAGE_luci-mod-firewall=y
          CONFIG_PACKAGE_luci-mod-system=y
          CONFIG_PACKAGE_luci-mod-status=y
          CONFIG_PACKAGE_luci-i18n-base-zh-cn=y
          CONFIG_PACKAGE_luci-theme-bootstrap=y
          CONFIG_PACKAGE_luci-theme-argon=y
          # Docker相关
          CONFIG_PACKAGE_docker=y
          CONFIG_PACKAGE_dockerd=y
          CONFIG_PACKAGE_docker-compose=y
          CONFIG_PACKAGE_luci-app-dockerman=y
          CONFIG_PACKAGE_luci-i18n-dockerman-zh-cn=y
          CONFIG_PACKAGE_libnetwork=y
          CONFIG_PACKAGE_kmod-nft-fib=y
          CONFIG_PACKAGE_kmod-nf-flow=y
          CONFIG_PACKAGE_kmod-nft-nat=y
          CONFIG_PACKAGE_kmod-nft-offload=y
          CONFIG_PACKAGE_kmod-ipt-nat6=y
          CONFIG_PACKAGE_kmod-ipt-raw=y
          CONFIG_PACKAGE_kmod-ipt-udp=y
          CONFIG_PACKAGE_kmod-ipt-conntrack=y
          CONFIG_PACKAGE_kmod-ipt-nat=y
          CONFIG_PACKAGE_kmod-libcgroup=y
          CONFIG_PACKAGE_kmod-overlay=y
          CONFIG_PACKAGE_kmod-veth=y
          # IPsec VPN基础
          CONFIG_PACKAGE_luci-app-ipsec-vpn=y
          CONFIG_PACKAGE_luci-i18n-ipsec-vpn-zh-cn=y
          CONFIG_PACKAGE_xl2tpd=y
          CONFIG_PACKAGE_ppp-mod-pptp=y
          CONFIG_PACKAGE_luci-proto-ppp=y
          CONFIG_PACKAGE_luci-proto-l2tp=y
          CONFIG_PACKAGE_kmod-ipsec=y
          CONFIG_PACKAGE_kmod-ipsec4=y
          CONFIG_PACKAGE_kmod-ipsec6=y
          CONFIG_PACKAGE_kmod-tun=y
          CONFIG_KERNEL_CRYPTO_AES=y
          CONFIG_KERNEL_CRYPTO_SHA1=y
          CONFIG_KERNEL_CRYPTO_HMAC=y
          CONFIG_KERNEL_CRYPTO_XFRM=y
          CONFIG_KERNEL_XFRM_USER=y
          CONFIG_KERNEL_XFRM_IPCOMP=y
          CONFIG_KERNEL_NET_KEY=y
          CONFIG_KERNEL_INET_AH=y
          CONFIG_KERNEL_INET_ESP=y
          CONFIG_KERNEL_INET_IPCOMP=y
          CONFIG_KERNEL_INET_XFRM_MODE_TRANSPORT=y
          CONFIG_KERNEL_INET_XFRM_MODE_TUNNEL=y
          CONFIG_PACKAGE_iptables-mod-extra=y
          CONFIG_PACKAGE_iptables-mod-ipopt=y
          CONFIG_PACKAGE_ip6tables=y
          CONFIG_PACKAGE_ipip=y
          CONFIG_PACKAGE_vxlan=y
          CONFIG_PACKAGE_luci-proto-ipip=y
          CONFIG_PACKAGE_luci-proto-vxlan=y
          CONFIG_PACKAGE_luci-proto-ipv6=y
          # 其他基础
          CONFIG_PACKAGE_ttyd=y
          CONFIG_PACKAGE_ucode-mod-socket=y
          # DDNS
          CONFIG_PACKAGE_luci-app-ddns=y
          CONFIG_PACKAGE_ddns-scripts=y
          CONFIG_PACKAGE_ddns-scripts-cloudflare=y
          # UDPXY
          CONFIG_PACKAGE_luci-app-udpxy=y
          # 防火墙
          CONFIG_PACKAGE_luci-app-firewall=y
          CONFIG_PACKAGE_firewall=y
          CONFIG_PACKAGE_firewall4=y
          CONFIG_PACKAGE_nftables=y
          # OpenClash核心
          CONFIG_PACKAGE_luci-app-openclash=y
          CONFIG_PACKAGE_luci-i18n-openclash-zh-cn=y
          CONFIG_PACKAGE_clash-meta=y
          CONFIG_PACKAGE_luci-app-unblockneteasemusic=y
          CONFIG_PACKAGE_luci-app-passwall=n
          EOF
          make defconfig
          echo "✅ 配置完成"
          grep -c "=" .config || true

      - name: 列出所有luci-app-*插件编译状态
        run: |
          echo -e "\n=================================================="
          echo -e "📋 所有 luci-app-* 插件（已选中 ✔️ | 未选中 ❌）"
          echo -e "==================================================\n"

          # 读取所有luci-app-*插件（包含选中+未选中）
          if [ -f .config ]; then
            grep -E '^(CONFIG_PACKAGE_luci-app-.*=|# CONFIG_PACKAGE_luci-app-.* is not set)' .config | sort | while read -r line; do
              if echo "$line" | grep -q '=y\|=m'; then
                # 已选中编译
                name=$(echo "$line" | sed -e 's/^CONFIG_PACKAGE_//' -e 's/=[ym]$//')
                echo " ✔️ $name"
              else
                # 未选中编译
                name=$(echo "$line" | sed -e 's/^# CONFIG_PACKAGE_//' -e 's/ is not set$//')
                echo " ❌ $name"
              fi
            done
          else
            echo " ⚠️ 未找到.config配置文件"
          fi
          echo -e "==================================================\n"

      - name: Advanced CMake patch compatibility check
        id: patch-check
        run: |
          echo "============================================================="
          echo "🔬 进阶检测：CMake 补丁兼容性分析"
          echo "============================================================="

          # 1. 读取当前 Makefile 中定义的 CMake 版本
          CMAKE_MK="./tools/cmake/Makefile"
          if [ ! -f "$CMAKE_MK" ]; then
            echo "::error::未找到 $CMAKE_MK"
            exit 1
          fi
          CURRENT_VERSION=$(grep -E '^PKG_VERSION:=' "$CMAKE_MK" | cut -d '=' -f2 | tr -d ' ')
          echo "📌 当前 Makefile 中的 CMake 版本: $CURRENT_VERSION"

          # 2. 获取目标检测版本（优先使用输入，否则获取最新 3.x 版）
          TARGET_VERSION="${{ inputs.cmake_version }}"
          if [ -z "$TARGET_VERSION" ]; then
            echo "🔍 未指定目标版本，正在获取最新 CMake 3.x 稳定版..."
            API_URL="https://api.github.com/repos/Kitware/CMake/releases?per_page=100"
            if [ -n "$GITHUB_TOKEN" ]; then
              AUTH_HEADER="Authorization: token $GITHUB_TOKEN"
            else
              AUTH_HEADER=""
            fi
            TARGET_VERSION=$(curl -s ${AUTH_HEADER:+-H "$AUTH_HEADER"} "$API_URL" | \
              jq -r '.[] | select(.tag_name | test("^v3\\.")) | select(.prerelease == false) | .tag_name' | \
              head -n1 | sed 's/^v//')
            if [ -z "$TARGET_VERSION" ]; then
              echo "::error::无法获取最新版本，请检查网络或 API 限制。"
              exit 1
            fi
          fi
          echo "🎯 检测目标版本: $TARGET_VERSION"
          echo "target_version=$TARGET_VERSION" >> $GITHUB_OUTPUT

          # 3. 下载目标版本的 Utilities/cmcurl/CMakeLists.txt 文件
          TMP_DIR=$(mktemp -d)
          cd "$TMP_DIR"
          FILE_URL="https://raw.githubusercontent.com/Kitware/CMake/v${TARGET_VERSION}/Utilities/cmcurl/CMakeLists.txt"
          echo "📥 下载文件: $FILE_URL"
          if ! curl -fsSL "$FILE_URL" -o CMakeLists.txt; then
            echo "::error::下载失败，请确认版本 $TARGET_VERSION 存在且文件路径未变更。"
            exit 1
          fi
          echo "✅ 文件下载成功"

          # 4. 显示 clock_gettime 相关上下文
          echo ""
          echo "📋 Utilities/cmcurl/CMakeLists.txt 中 clock_gettime 的上下文:"
          grep -n -B 2 -A 5 "clock_gettime" CMakeLists.txt || echo "⚠️ 未找到 clock_gettime 相关内容"

          # 5. 分析关键补丁 120-curl-fix-libressl-linking.patch
          PATCH_FILE="${GITHUB_WORKSPACE}/tools/cmake/patches/120-curl-fix-libressl-linking.patch"
          echo ""
          echo "🔧 分析补丁: $(basename "$PATCH_FILE")"

          if [ ! -f "$PATCH_FILE" ]; then
            echo "⚠️ 补丁文件不存在，跳过分析。"
          else
            # 提取补丁期望的行号和上下文
            HUNK_LINE=$(grep -E '^@@ ' "$PATCH_FILE" | head -n1)
            EXPECTED_LINE=$(echo "$HUNK_LINE" | sed -n 's/.*-\([0-9]*\),.*/\1/p')
            echo "补丁期望修改起始行号: $EXPECTED_LINE"

            # 显示补丁中的关键上下文（前几行）
            echo "补丁上下文片段:"
            grep -A 8 '^@@ ' "$PATCH_FILE" | head -n 10

            # 6. 对比实际行号
            ACTUAL_LINE=$(grep -n "check_symbol_exists(clock_gettime" CMakeLists.txt | cut -d ':' -f1 | head -n1)
            if [ -z "$ACTUAL_LINE" ]; then
              echo "❌ 未在目标文件中找到 clock_gettime，补丁可能已过时。"
              echo "compatible=false" >> $GITHUB_OUTPUT
            elif [ "$EXPECTED_LINE" -eq "$ACTUAL_LINE" ]; then
              echo "✅ 行号完全匹配 (期望 $EXPECTED_LINE = 实际 $ACTUAL_LINE)，补丁可能可直接应用。"
              echo "compatible=true" >> $GITHUB_OUTPUT
            else
              echo "⚠️ 行号不匹配 (期望 $EXPECTED_LINE ≠ 实际 $ACTUAL_LINE)，补丁需要手动调整。"
              echo "compatible=false" >> $GITHUB_OUTPUT
            fi
          fi

          # 7. 清理临时目录
          cd "${GITHUB_WORKSPACE}"
          rm -rf "$TMP_DIR"
          echo "============================================================="
      
      - name: Build OpenWrt firmware
        id: build
        run: |
          set +e
          echo "开始编译 OpenWrt 固件 (x86_64) - 多线程模式..."
          # 多线程编译（无详细日志）
          make -j$(nproc) 2>&1 | tee build.log
          BUILD_RESULT=${PIPESTATUS[0]}
          
          # 多线程编译失败 → 自动单线程重编+输出详细日志
          if [ $BUILD_RESULT -ne 0 ]; then
            echo -e "\n❌ 多线程编译失败，切换单线程+详细日志重编..."
            make clean
            make -j1 V=s 2>&1 | tee -a build.log
            BUILD_RESULT=${PIPESTATUS[0]}
          fi
          
          set -e
          
          # 显示 ccache 最终使用状态
          ccache -s
          
          # 最终构建状态判断
          if [ $BUILD_RESULT -eq 0 ]; then
            echo "✅ 编译成功"
            echo "build_status=success" >> $GITHUB_OUTPUT
            exit 0
          else
            echo "❌ 编译失败"
            echo "build_status=failed" >> $GITHUB_OUTPUT
            exit 1
          fi
      
      - name: Compress squashfs firmware
        if: steps.build.outputs.build_status == 'success'
        run: |
          echo "📦 开始压缩 squashfs 固件文件..."
          
          # 遍历压缩所有架构的 squashfs 固件
          for firmware_file in bin/targets/*/*/*squashfs*; do
            if [ -f "${firmware_file}" ]; then
              echo "✅ 压缩固件: ${firmware_file}"
              gzip "${firmware_file}"
            fi
          done
          
          echo ""
          echo "📊 压缩完成，生成文件列表:"
          ls -lh bin/targets/*/*/*squashfs*.gz 2>/dev/null || true

      - name: Upload compressed squashfs firmware
        if: steps.build.outputs.build_status == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: openwrt-squashfs-${{ needs.prepare.outputs.build_time }}
          path: bin/targets/*/*/*squashfs*.gz
          retention-days: 30

      - name: Upload build config file
        if: steps.build.outputs.build_status == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: openwrt-build-config
          path: .config
          retention-days: 30

      - name: Upload build logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs
          path: build.log
          retention-days: 7

      - name: Show build summary on failure
        if: failure()
        run: |
          echo "❌ 编译失败 - 显示最后 200 行构建日志:"
          tail -n 200 build.log

  build-summary:
    name: Build Summary
    runs-on: ubuntu-latest
    needs: [prepare, build-firmware]
    if: always()
    
    steps:
      - name: Generate build summary
        run: |
          cat >> $GITHUB_STEP_SUMMARY << 'EOF'
          # 📦 OpenWrt x86_64 Firmware Build
          
          **Build Status:** ${{ needs.build-firmware.result }}
          **Build Time:** ${{ needs.prepare.outputs.build_time }}
          
          ## Target Configuration
          - Platform: x86_64 Generic
          - Branch: fanchmwrt-25.12.2
          - Kernel Partition: 100 MB
          - Root FS: 1024 MB (Ext4)
          
          ## 🎵 Core Applications
          | App | Description | Source |
          |-----|-------------|--------|
          | luci-app-openclash | OpenClash 代理工具 | vernesong/OpenClash |
          | luci-app-dockerman | Docker 管理界面 | lisaac/luci-app-dockerman |
          
          ## 📡 Additional Services
          | Category | Packages |
          |----------|----------|
          | VPN | IPsec, L2TP, PPTP |
          | Container | Docker, Docker Compose |
          | Utilities | ttyd, DDNS, UDPXY |
          | Firewall | Firewall4 (nftables) |
          
          ## 📊 Build Info
          - Feeds: Official OpenWrt + 第三方插件源
          - Included: 基础工具 + 网络套件 + LuCI中文界面
          - Compilation: 并行编译 + 失败自动单线程重编 + ccache 缓存加速
          
          [View Workflow Run →](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})
          EOF
